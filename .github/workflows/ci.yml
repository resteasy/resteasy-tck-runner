# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: RESTEasy Jakarta REST 3.1 TCK

on:
  push:
    branches:
      - 'jakarta-rest-3.1'
    paths:
      - '**/pom.xml'
      - 'resteasy-tck-adapter/**'
      - 'resteasy-tck-setup/**'
  pull_request:
    branches:
      - '**'
    paths:
      - '.github/workflows/ci.yml'
      - '**/pom.xml'
      - 'resteasy-tck-adapter/**'
      - 'resteasy-tck-setup/**'

# Only run the latest job
concurrency:
  group: '${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        java: ['17', '21']

    steps:
      - uses: actions/checkout@v5
      - name: Jakarta REST 3.1 JDK-${{ matrix.java }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: 'maven'
      - name: Build with Java ${{ matrix.java }}
        run:  |
          mvn -ntp clean install -U -B -fae
      - uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: surefire-reports-${{ matrix.java }}
          path: '**/failsafe-reports/'
      - uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: server-logs-${{ matrix.java }}
          path: '**/server.log'
  build-jdk11:

    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v5
      - name: Set up JDK 11
        uses: actions/setup-java@v5
        with:
          java-version: 11
          distribution: 'temurin'
          cache: 'maven'
      - name: Jakarta REST 3.1 JDK-11
        # Wildfly 34 is the last version to support Java 11
        run:  |
          mvn -ntp clean install -U -B -fae -Dversion.org.wildfly=34.0.1.Final
      - uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: surefire-reports-11
          path: '**/failsafe-reports/'
      - uses: actions/upload-artifact@v5
        if: failure()
        with:
          name: server-logs-11
          path: '**/server.log'
